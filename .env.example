# Copy to `.env.local` to change behaviour locally. Every variable is optional:
# the app is fully functional with no environment file at all.
#
# Opt in to sending Core Web Vitals beacons to `/__telemetry` (see src/lib/perf.js).
VITE_TELEMETRY=off
